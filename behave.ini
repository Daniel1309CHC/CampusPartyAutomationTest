[behave]
format = pretty
outfiles = reports/pretty.output
logging_level = WARNING
log_capture = True
log_capture_format = %(asctime)s - %(levelname)s - %(message)s
junit = True
junit_directory = reports
paths = tests/features